import { ButtonData } from '../../ui/Button/types';
import { TitleData } from '../../ui/Title/types';

export type CashbackData = {
  title: TitleData;
  texts: string[];
  buttonText: ButtonData;
};
